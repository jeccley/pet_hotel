<%= bootstrap_form_with(model: [customer, booking], local: true, label_errors: true) do |form| %>
  <%= form.date_field :drop_off %>
  <%= form.date_field :pick_up %>
  <%= form.text_area :notes, placeholder: "Additional notes about the booking" %>
  <%= form.primary %>
<% end %>